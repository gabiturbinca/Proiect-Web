# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=proiect_web
DB_USER=yourname
DB_PASS=yourpass

# Application environment: development | production
APP_ENV=production

# poti folosi
#   php -r "echo base64_encode(random_bytes(32));"
JWT_SECRET=secret
